
注意: 这个方式似乎不能处理null, 如果返回值是null的话,不走增强


1. 可以放回一个空字符串或者0或者任意什么之类的(一定要是没有意义的,你能给前段造成歧义)
2. 可以手动返回一个response, 这个就不用走增强了, 手动返回的response里面data可以是null
3. 其实spring对于null为什么不处理呢? 我们任何时候都不应该返回一个null,不是吗? null难道不是一个异常吗? 是的. null是一种异常. 如果查询不到数据,我们可以返回一个空的list或者告诉前端参数有问题(没有查询到数据)->可以当做一个异常处理(异常是走增强的)






